[phases.setup]
nixPkgs = ["python312Full"]

[phases.install]
cmds = [
  "python -m pip install -r requirements.txt"
]

[start]
cmd = "cd /app && PYTHONPATH=/app/src:$PYTHONPATH uvicorn research_intel.run_server:app --host 0.0.0.0 --port $PORT"
