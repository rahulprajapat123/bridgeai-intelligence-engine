[phases.setup]
nixPkgs = ["python312", "python312Packages.pip"]
nixLibs = ["stdenv.cc.cc.lib"]

[phases.install]
cmds = [
  "pip install -r requirements.txt"
]

[start]
cmd = "PYTHONPATH=/app/src:$PYTHONPATH uvicorn research_intel.run_server:app --host 0.0.0.0 --port $PORT"
